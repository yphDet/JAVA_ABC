【1】：创建String对象的两种方式
1.1：方式一：直接赋值String s = "hspedu";//指向常量池
1.2：方式二：调用构造器String s = new String("hspedu");//指向对象，对象中的value属性指向常量池

200% 【2】：两种创建String对象的区别
方式一：直接赋值String s = "hsp";
方式二：调用构造器String s2 = new String("hsp");

2.1：方式一：向从常量池查看是否有"hsp"数据空间，如果有，直接指向；
如果没有，则创建，然后指向（地址）。s最终指向的是常量池的空间地址

2.2：方式二：先在堆中创建空间（对new的底层操作），里面维护了value属性（String类用于存放字符串内容）
指向常量池的 hsp空间。如果常量池没有“hsp”，重新创建，如果有，直接通过value指向。最终指向的是堆中的空间地址

2.3：画出两种方式的内存分布图；（必须掌握）------------！！！！重要

//小结：
变量的指向不同（常量池 或 堆中的对象）