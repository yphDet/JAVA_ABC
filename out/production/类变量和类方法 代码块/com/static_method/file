1、细节
80% 2、类方法和普通方法都是随着类的加载而加载，将结构信息存储在方法区

100% 2.1：类方法中不允许使用和对象有关的关键字，比如this和super。
【static共享与对象没有关系，因此有与对象含义的关键字都无法使用】


100% 3、类方法与普通成员方法的区别
3、1 类方法（静态方法）中 只能访问 静态方法 或 类变量
3、2 普通成员方法，既可以访问 普通变量（方法），也可以访问静态变量（方法）

4）、类方法可以通过类名调用，也可以通过对象名调用。

5）、普通方法和对象有关，需要通过对象名调用。