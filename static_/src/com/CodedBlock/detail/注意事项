[修饰符]{
    代码
    }
说明注意：
1）修饰符只能为static

2）代码块  分为两类，使用static修饰的叫静态代码，没有static修饰的，叫普通代码块/非静态代码块
3）逻辑语句可以为任何逻辑语句（输入、输出、方法调用、循环、判断等）

理解：
1）相当于另外一种形式的构造器（对构造器的补充机制），可以做初始化的操作
2）提高代码的重用性【还是利用到了static来修饰代码块，[类只会加载一次]】