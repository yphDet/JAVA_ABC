类加载一定是在对象创建之前。

静态与类加载有直接关系，静态是共有性质的（有范围：同包本类以及同包本类的任一对象）

类加载 与 类变量的初始化 以及 static代码块 有关。     与类方法无关。

一个类只会加载一次；

静态的（代码块，变量{初始化}） 》 普通的 》 普通的 》构造器 【继承中父类优先】

静态方法不能使用与对象有关的关键字（如：this、super等）

代码块要想使用修饰符只能是static

目前学类中的五大成员中的四个 ——》 变量，方法，构造器，代码块
